<p id="notice"><%= notice %></p>

<h1>Here are your Best Shots at Happiness!</h1>

<h2>
  <% @move.cities.each do |city| %>
    <%= city.name%><br />
  <% end %>
</h2>
<%= link_to 'Edit', edit_move_path(@move) %> |
<%= link_to 'Back', moves_path %>
